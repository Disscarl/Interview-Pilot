fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# Code targets LangChain 1.x / langchain-core 1.5 APIs (see agent/graph.py).
langchain>=1.2,<2
langchain-openai>=1.0,<2
langchain-anthropic>=1.0,<2
# Direct import in agent/graph.py — declared explicitly, not via langchain's
# transitive dependency, so a langchain upgrade can never silently drop it.
langgraph>=1.2.11,<1.3.0
pydantic>=2.0
aiosqlite>=0.20.0
python-dotenv>=1.0
httpx>=0.27.0
pdfplumber>=0.11.0
