"""Deprecated stub.

The interview flow is a plain LangChain LLM chain (see agent/interviewer.py),
not a tool-calling agent, so these @tool helpers were removed. This file is
left empty because file deletion is unavailable in the current environment; it
is not imported anywhere.
"""
